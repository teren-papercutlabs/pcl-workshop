---
name: interview
description: "Backward-compatible alias for the workshop disambiguation flow. Use when participants ask for interviews, structured discovery, requirements clarification, or problem scoping regardless of specific wording. Immediately follow the disambiguate protocol; do not run a separate interview process."
---

# Interview

`/interview` is now an alias for `/disambiguate`.

Use `../disambiguate/SKILL.md` as the source of truth. Do not run a second, separate interview protocol.

If the participant invoked `/interview`, say:

> "I'll use the disambiguation flow for this: first I’ll restate the problem, then I’ll propose concrete statements or short questions for you to react to."

Then follow `/disambiguate` and write `problem-brief.md`.
